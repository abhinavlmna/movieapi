ListTile(
            leading: Image.network(
              movie.poster,
              width: 100,
              height: 150,
              fit: BoxFit.cover,
            ),
            title: Text(movie.title),
            subtitle: Text('${movie.year} | ${movie.genre.join(', ')}'),
            onTap: () {},
          );